{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"NovaX","text":"<p>GPU-accelerated math library for Python \u2014 tensors, neural-network ops, and autograd in one package.</p> <ul> <li> <p>:material-lightning-bolt:{ .lg .middle } GPU-Accelerated by Default</p> <p>Every operation dispatches to an optimized CUDA kernel when a GPU is present, with transparent CPU fallback via NumPy \u2014 no code changes required.</p> </li> <li> <p>:material-brain:{ .lg .middle } Neural-Network Ready</p> <p>Activations, reductions, matrix multiplication, and reverse-mode automatic differentiation are all first-class citizens.</p> </li> <li> <p>:material-graph:{ .lg .middle } Lazy Expression Graphs</p> <p>Operators build a computation graph. Calling <code>.eval()</code> compiles and fuses the graph into a single CUDA kernel, reducing memory round-trips.</p> </li> <li> <p>:material-memory:{ .lg .middle } Bucketed Memory Pool</p> <p>GPU buffers are recycled in O(1) using power-of-2 size bins \u2014 allocation overhead disappears in tight training loops.</p> </li> </ul>"},{"location":"#install","title":"Install","text":"CPU onlyGPU (PyCUDA + CUDA)From source <pre><code>pip install novax\n</code></pre> <pre><code>pip install \"novax[gpu]\"\n</code></pre> <pre><code>git clone https://github.com/SS-012/novax.git\ncd novax\npip install -e \".[gpu]\"\n</code></pre>"},{"location":"#quick-examples","title":"Quick Examples","text":""},{"location":"#elementwise-math","title":"Elementwise math","text":"<pre><code>import novax as nx\nimport numpy as np\n\nx = nx.Tensor(np.array([1.0, 2.0, 3.0, 4.0]))\n\n# Operator overloading builds a lazy graph; .eval() executes it\ny = (x * 2 - 1).eval()\nprint(y.data)   # [1. 3. 5. 7.]\n\n# Unary ops\nprint(nx.relu(x - 3).eval().data)    # [0. 0. 0. 1.]\nprint(nx.exp(nx.log(x)).eval().data) # [1. 2. 3. 4.]\n</code></pre>"},{"location":"#neural-network-forward-backward-pass","title":"Neural-network forward + backward pass","text":"<pre><code>import novax as nx\nimport numpy as np\n\nnp.random.seed(0)\nx  = nx.Tensor(np.random.randn(32, 16).astype(np.float32))\nW1 = nx.Tensor(np.random.randn(16, 8).astype(np.float32), requires_grad=True)\nb1 = nx.Tensor(np.zeros(8, dtype=np.float32),             requires_grad=True)\nW2 = nx.Tensor(np.random.randn(8, 1).astype(np.float32),  requires_grad=True)\n\n# Forward pass\nh    = nx.relu(nx.matmul(x, W1) + b1)   # (32, 8)\nout  = nx.matmul(h, W2)                  # (32, 1)\nloss = nx.mean(out)\n\n# Backward pass\nloss.eval().backward()\n\nprint(W1.grad.shape)  # (16, 8)\nprint(W2.grad.shape)  # (8, 1)\nprint(b1.grad.shape)  # (8,)\n</code></pre>"},{"location":"#gpu-round-trip","title":"GPU round-trip","text":"<pre><code>a = nx.Tensor(np.random.randn(1_000_000).astype(np.float32))\na.to_gpu()                          # upload to VRAM\n\nresult = nx.sigmoid(a).eval()       # CUDA kernel, stays on GPU\nhost   = result.to_host()           # download only when you need it\nprint(host.shape)                   # (1000000,)\n</code></pre>"},{"location":"#version","title":"Version","text":"<p>Current stable release: 0.2.0 \u2014 see the Changelog for what's new.</p>"},{"location":"changelog/","title":"Changelog","text":""},{"location":"changelog/#020-2026-05-24","title":"[0.2.0] \u2014 2026-05-24","text":""},{"location":"changelog/#added","title":"Added","text":"<p>Math operations</p> <ul> <li><code>exp</code>, <code>log</code>, <code>sqrt</code>, <code>abs</code>, <code>neg</code> \u2014 unary elementwise ops with CPU (NumPy) and GPU (CUDA) backends</li> <li><code>pow</code> \u2014 elementwise power (<code>a ** b</code>), binary, with full gradient support</li> <li><code>__neg__</code>, <code>__pow__</code>, <code>__matmul__</code> operator overloads on <code>Tensor</code></li> <li>Reverse operators: <code>__radd__</code>, <code>__rsub__</code>, <code>__rmul__</code>, <code>__rtruediv__</code></li> </ul> <p>Activation functions</p> <ul> <li><code>relu</code> \u2014 <code>max(0, x)</code>, CUDA: <code>fmaxf(0.0f, a[idx])</code></li> <li><code>sigmoid</code> \u2014 <code>1 / (1 + exp(-x))</code>, CUDA: <code>1.0f / (1.0f + expf(-a[idx]))</code></li> <li><code>tanh</code> \u2014 CUDA: <code>tanhf(a[idx])</code></li> <li><code>softmax</code> \u2014 numerically stable, three-pass GPU implementation</li> </ul> <p>Reduction operations</p> <ul> <li><code>sum</code>, <code>mean</code>, <code>max</code>, <code>min</code> \u2014 reduce over all elements to a scalar <code>(1,)</code> tensor</li> <li>GPU: two-pass parallel tree reduction using 256-thread blocks with shared memory</li> </ul> <p>Linear algebra</p> <ul> <li><code>matmul</code> / <code>@</code> \u2014 matrix multiplication, CPU via <code>np.matmul</code>, GPU via 16\u00d716 tiled CUDA kernel</li> <li><code>launch_matmul_bias_relu</code> \u2014 fused matmul + bias + ReLU CUDA kernel (no extra memory round-trips)</li> </ul> <p>Automatic differentiation</p> <ul> <li><code>requires_grad</code> parameter on <code>Tensor</code></li> <li><code>Tensor.grad</code> \u2014 accumulated gradient after <code>backward()</code></li> <li><code>Tensor.backward()</code> \u2014 reverse-mode autodiff via topological sort</li> <li><code>no_grad</code> context manager \u2014 disables gradient tracking in its scope</li> <li>Broadcasting gradient correction \u2014 <code>_unbroadcast()</code> sums gradients back to input shapes</li> <li>Gradient rules for all 19 ops</li> </ul> <p>Shape manipulation</p> <ul> <li><code>Tensor.reshape(*new_shape)</code> \u2014 reshape tensor, downloads from GPU if needed</li> <li><code>Tensor.transpose(axes=None)</code> \u2014 NumPy-compatible axis transpose</li> </ul> <p>Infrastructure</p> <ul> <li>Bucketed GPU memory pool \u2014 O(1) alloc/free using power-of-2 size bins (replaces linear scan)</li> <li>Adaptive CUDA block size \u2014 queries <code>MAX_THREADS_PER_BLOCK</code> per device, selects 512/256/128</li> <li>Kernel fusion extended to include unary ops (<code>exp</code>, <code>relu</code>, <code>sigmoid</code>, etc.) inside fused graphs</li> <li><code>_is_lazy()</code> check in dispatch layer \u2014 unary and binary functions short-circuit correctly for lazy inputs</li> </ul>"},{"location":"changelog/#changed","title":"Changed","text":"<ul> <li><code>eval()</code> refactored into <code>_eval_unary()</code>, <code>_eval_binary()</code>, <code>_eval_matmul()</code> helpers</li> <li><code>to_gpu()</code> now guards against double-upload; clears host data from pool buffers</li> <li><code>dispatch.py</code> module restructured: each function handles lazy path and eager path with grad setup</li> </ul>"},{"location":"changelog/#tests","title":"Tests","text":"<ul> <li>101 tests passing; 13 GPU tests skipped when CUDA unavailable</li> <li>New <code>tests/test_phase1_ops.py</code> \u2014 coverage for all new ops, reshape, transpose, eval paths</li> <li>New <code>tests/test_autograd.py</code> \u2014 numerical gradient checks (finite differences) for all differentiable ops</li> </ul>"},{"location":"changelog/#0116-2025","title":"[0.1.16] \u2014 2025","text":""},{"location":"changelog/#fixed","title":"Fixed","text":"<ul> <li>CPU op dispatch fallback was missing for several ops; added explicit fallback chain</li> <li>Kernel cache was not keyed on CUDA source; identical names but different expressions could collide</li> <li><code>to_gpu()</code> guard order was inverted \u2014 could attempt GPU upload without checking <code>GPU_AVAILABLE</code> first</li> <li><code>eval()</code> binary GPU path was missing a <code>return</code> statement, falling through to CPU unnecessarily</li> </ul>"},{"location":"changelog/#added_1","title":"Added","text":"<ul> <li>CI via GitHub Actions \u2014 Python 3.10/3.11/3.12 matrix with <code>pytest --cov</code></li> <li><code>tests/test_novax.py</code> \u2014 46 tests covering creation, ops, eval, fusion, mempool, and GPU round-trips</li> </ul>"},{"location":"changelog/#010-2025","title":"[0.1.0] \u2014 2025","text":"<p>Initial release.</p> <ul> <li><code>Tensor</code> class with CPU/GPU storage, lazy evaluation, and expression graph</li> <li>Binary elementwise ops: <code>add</code>, <code>sub</code>, <code>mul</code>, <code>div</code></li> <li>GPU kernel launcher with PyCUDA, CUDA source-module cache</li> <li>Expression fusion \u2014 <code>_build_fused()</code> compiles multi-op chains into a single kernel</li> <li>GPU memory pool (<code>utils/mempool.py</code>)</li> <li><code>set_default_device()</code> for runtime device selection</li> </ul>"},{"location":"concepts/","title":"Core Concepts","text":""},{"location":"concepts/#lazy-evaluation-and-expression-graphs","title":"Lazy Evaluation and Expression Graphs","text":"<p>NovaX operators do not execute immediately. Instead, every arithmetic operation returns a new <code>Tensor</code> that records the operation and its inputs \u2014 forming a directed acyclic graph (DAG).</p> <pre><code>a = nx.Tensor([1.0, 2.0, 3.0])\nb = nx.Tensor([4.0, 5.0, 6.0])\n\nc = a + b       # builds a graph node, no CUDA launched yet\nd = c * 2.0     # extends the graph\n</code></pre> <p>The graph is executed by calling <code>.eval()</code>:</p> <pre><code>result = d.eval()   # compiles graph \u2192 executes \u2192 returns concrete Tensor\n</code></pre> <p>This design enables:</p> <ol> <li>Kernel fusion \u2014 a chain of elementwise ops gets compiled into a single CUDA kernel</li> <li>Constant folding \u2014 operations on constants are evaluated at compile time</li> <li>Device-agnostic code \u2014 the graph records intent; dispatch decides CPU vs GPU at <code>eval()</code> time</li> </ol>"},{"location":"concepts/#kernel-fusion","title":"Kernel Fusion","text":"<p>When <code>eval()</code> encounters a chain of elementwise ops, it calls <code>_build_fused()</code> to emit a single CUDA C expression, then compiles and launches one kernel instead of many.</p> <pre><code>a + b * exp(c)\n</code></pre> <p>becomes the single kernel body:</p> <pre><code>out[idx] = a[idx] + b[idx] * expf(c[idx]);\n</code></pre> <p>Compiled kernels are cached by <code>(name, source)</code> so repeated calls with the same graph structure pay zero recompilation cost.</p>"},{"location":"concepts/#constant-folding","title":"Constant Folding","text":"<p>Before building the fused kernel, NovaX walks the graph with <code>_fold_constants()</code>. Subgraphs consisting entirely of scalar constants are collapsed at Python time:</p> <pre><code>x = nx.Tensor([1.0, 2.0])\ny = x + nx.Tensor(2.0) * nx.Tensor(3.0)   # 2.0 * 3.0 folded to 6.0\n</code></pre> <p>The kernel that reaches the GPU is simply <code>x[idx] + 6.0f</code>.</p>"},{"location":"concepts/#dispatch-cpu-vs-gpu","title":"Dispatch: CPU vs GPU","text":"<p>The <code>dispatch.py</code> module routes every function call to the correct backend:</p> <pre><code>nx.relu(a)\n  \u2514\u2500 _exec_unary(\"relu\", gpu_relu, cpu_relu, a)\n       \u251c\u2500 GPU: if a.on_gpu \u2192 launch CUDA kernel\n       \u2514\u2500 CPU: np.maximum(0, a.data)\n</code></pre> <p>Autograd backward closures are attached immediately after execution, before the result is returned, so the graph is always ready for <code>backward()</code>.</p>"},{"location":"concepts/#automatic-differentiation","title":"Automatic Differentiation","text":"<p>NovaX implements reverse-mode autodiff (backpropagation):</p> <ol> <li>Forward pass \u2014 each op attaches a <code>_backward</code> closure to the result tensor    and records the result's dependencies in <code>_prev</code></li> <li><code>.backward()</code> \u2014 topological sort of <code>_prev</code> links, then closures are called    in reverse order, accumulating gradients via <code>+=</code></li> <li>Broadcasting \u2014 if a tensor was broadcast during the forward pass, gradients    are summed back to the original shape</li> </ol> <pre><code>loss = mean(relu(matmul(x, W) + b))\n\nBackward graph:\n  loss \u2190 mean \u2190 relu \u2190 add \u2190 matmul(x, W)\n                         \u2514\u2500\u2500\u2500 b\n</code></pre> <p>Only tensors with <code>requires_grad=True</code> accumulate gradients. Intermediate nodes without it are skipped, keeping backward cheap.</p>"},{"location":"concepts/#gpu-memory-pool","title":"GPU Memory Pool","text":"<p>Every GPU buffer allocation goes through <code>novax.utils.mempool</code> instead of calling <code>cuda.mem_alloc</code> directly. The pool uses power-of-2 size bins:</p> <pre><code>8 B  \u2502 16 B \u2502 32 B \u2502 64 B \u2502 \u2026 \u2502 4 MB \u2502 \u2026\n []       [ptr1]     []      [ptr2, ptr3]\n</code></pre> <p>When a tensor is freed (<code>tensor.free()</code>), its buffer is returned to the matching bin. The next allocation of the same bucketed size pops the buffer in O(1) without touching the CUDA driver \u2014 eliminating allocation overhead in training loops.</p> <p>Set <code>release=True</code> to return memory to the CUDA driver instead of the pool:</p> <pre><code>tensor.free(release=True)\n</code></pre>"},{"location":"concepts/#thread-safety","title":"Thread Safety","text":"<p>The current implementation is single-threaded. The global <code>_grad_enabled</code> flag and the module-level kernel cache are not protected by locks. Multi-threaded training is not supported in v0.2.0.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#requirements","title":"Requirements","text":"Requirement Minimum Python 3.8+ NumPy 1.25+ CUDA toolkit (GPU only) 12.0+ PyCUDA (GPU only) 2024.1+"},{"location":"getting-started/#installation","title":"Installation","text":"CPU onlyGPU-enabledDevelopment <p>No GPU dependencies. Uses NumPy for all computations.</p> <pre><code>pip install novax\n</code></pre> <p>Requires a CUDA-capable GPU and the CUDA 12 toolkit already installed on your system.</p> <pre><code>pip install \"novax[gpu]\"\n</code></pre> <p>Includes the test suite and coverage tooling.</p> <pre><code>git clone https://github.com/SS-012/novax.git\ncd novax\npip install -e \".[dev]\"\n</code></pre>"},{"location":"getting-started/#verify-the-install","title":"Verify the Install","text":"<pre><code>import novax as nx\n\nprint(nx.__version__)        # 0.2.0\nprint(nx.GPU_AVAILABLE)      # True / False depending on your system\n</code></pre>"},{"location":"getting-started/#your-first-computation","title":"Your First Computation","text":"<p>NovaX tensors are created from NumPy arrays, Python lists, or scalars.</p> <pre><code>import novax as nx\nimport numpy as np\n\na = nx.Tensor([1.0, 2.0, 3.0])\nb = nx.Tensor([4.0, 5.0, 6.0])\n\n# Arithmetic builds a lazy graph\nc = a + b           # no computation yet\nprint(c.is_leaf)    # False \u2014 it's a graph node\n\n# .eval() compiles and executes the graph\nresult = c.eval()\nprint(result.data)  # [5. 7. 9.]\n</code></pre>"},{"location":"getting-started/#selecting-the-compute-device","title":"Selecting the Compute Device","text":"<p>NovaX automatically uses the GPU when tensors are on GPU memory. Use <code>to_gpu()</code> to upload and <code>to_host()</code> to download.</p> <pre><code>if nx.GPU_AVAILABLE:\n    a = nx.Tensor(np.ones(1024, dtype=np.float32))\n    a.to_gpu()               # now in VRAM\n\n    result = nx.exp(a).eval()\n    print(result.to_host())  # back to NumPy array\n</code></pre> <p>You can override the default device for all operations:</p> <pre><code>nx.set_default_device(\"cpu\")   # force CPU even when GPU is present\nnx.set_default_device(\"gpu\")   # default when GPU is available\n</code></pre>"},{"location":"getting-started/#gradients-in-60-seconds","title":"Gradients in 60 Seconds","text":"<p>Mark tensors with <code>requires_grad=True</code>, run a forward pass, call <code>.backward()</code>.</p> <pre><code>import novax as nx\nimport numpy as np\n\nx = nx.Tensor(np.array([2.0, 3.0]), requires_grad=True)\n\n# y = sum(x^2)  \u2192  dy/dx = 2x\ny = nx.sum(x ** 2)\ny.eval().backward()\n\nprint(x.grad.data)  # [4. 6.]\n</code></pre> <p>To temporarily disable gradient tracking:</p> <pre><code>with nx.no_grad():\n    z = nx.relu(x).eval()   # no backward closure attached\n</code></pre>"},{"location":"getting-started/#next-steps","title":"Next Steps","text":"<ul> <li>Core Concepts \u2014 lazy graphs, fusion, memory pool</li> <li>Tensor API \u2014 full reference for the <code>Tensor</code> class</li> <li>API Reference \u2014 all ops, activations, reductions, autograd</li> </ul>"},{"location":"api/activations/","title":"Activation Functions","text":"<p>Pointwise nonlinearities commonly used as hidden-layer activations in neural networks.</p>"},{"location":"api/activations/#novaxrelu","title":"<code>novax.relu</code>","text":"<pre><code>novax.relu(a) -&gt; Tensor\n</code></pre> <p>Rectified Linear Unit. Sets negative values to zero.</p> \\[\\text{relu}(x) = \\max(0,\\, x)\\] <pre><code>a = nx.Tensor([-2.0, -1.0, 0.0, 1.0, 2.0])\nnx.relu(a).eval().data   # [0. 0. 0. 1. 2.]\n</code></pre> <p>GPU kernel: <code>fmaxf(0.0f, a[idx])</code></p> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}} \\cdot \\mathbf{1}[a &gt; 0]\\] <p>The gradient is zero for all non-positive inputs (the \"dead neuron\" problem does not get special handling in NovaX \u2014 that is left to the caller's initialisation and learning-rate choices).</p>"},{"location":"api/activations/#novaxsigmoid","title":"<code>novax.sigmoid</code>","text":"<pre><code>novax.sigmoid(a) -&gt; Tensor\n</code></pre> <p>Logistic sigmoid. Squashes values into \\((0, 1)\\).</p> \\[\\sigma(x) = \\frac{1}{1 + e^{-x}}\\] <pre><code>a = nx.Tensor([-2.0, 0.0, 2.0])\nnx.sigmoid(a).eval().data   # [0.119  0.5    0.881]\n</code></pre> <p>GPU kernel: <code>1.0f / (1.0f + expf(-a[idx]))</code></p> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}} \\cdot \\sigma(a)\\,(1 - \\sigma(a))\\]"},{"location":"api/activations/#novaxtanh","title":"<code>novax.tanh</code>","text":"<pre><code>novax.tanh(a) -&gt; Tensor\n</code></pre> <p>Hyperbolic tangent. Squashes values into \\((-1, 1)\\).</p> \\[\\tanh(x) = \\frac{e^x - e^{-x}}{e^x + e^{-x}}\\] <pre><code>a = nx.Tensor([-1.0, 0.0, 1.0])\nnx.tanh(a).eval().data   # [-0.762  0.     0.762]\n</code></pre> <p>GPU kernel: <code>tanhf(a[idx])</code></p> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}} \\cdot (1 - \\tanh^2(a))\\]"},{"location":"api/activations/#novaxsoftmax","title":"<code>novax.softmax</code>","text":"<pre><code>novax.softmax(a) -&gt; Tensor\n</code></pre> <p>Softmax normalisation over all elements of the tensor. Produces a probability distribution that sums to 1.</p> \\[\\text{softmax}(x)_i = \\frac{e^{x_i - \\max(x)}}{\\sum_j e^{x_j - \\max(x)}}\\] <p>The \\(-\\max(x)\\) shift is applied for numerical stability \u2014 it prevents overflow in the exponential without changing the output.</p> <pre><code>logits = nx.Tensor([2.0, 1.0, 0.1])\nprobs  = nx.softmax(logits).eval()\nprint(probs.data)              # [0.659  0.242  0.099]\nprint(probs.data.sum())        # 1.0\n</code></pre> <p>Axis</p> <p>The current implementation always reduces over all elements. Per-row softmax (axis=1) is not yet supported.</p> <p>GPU implementation</p> <p>On GPU, softmax is computed in three passes:</p> <ol> <li>Parallel reduction to find <code>max(x)</code></li> <li>Elementwise <code>exp(x - max)</code> kernel</li> <li>Parallel reduction to find the normalisation sum, followed by a division kernel</li> </ol> <p>Gradient</p> <p>Softmax backward is not yet implemented in the autograd engine. Call it in inference-only contexts, or outside a <code>requires_grad</code> scope.</p> <pre><code>with nx.no_grad():\n    probs = nx.softmax(logits).eval()\n</code></pre>"},{"location":"api/activations/#choosing-an-activation","title":"Choosing an Activation","text":"Activation Output range Common use <code>relu</code> \\([0, +\\infty)\\) Hidden layers, CNNs <code>sigmoid</code> \\((0, 1)\\) Binary classification output <code>tanh</code> \\((-1, 1)\\) RNNs, normalised hidden layers <code>softmax</code> \\((0, 1)\\), sum = 1 Multiclass classification output"},{"location":"api/activations/#example-two-layer-mlp","title":"Example \u2014 Two-layer MLP","text":"<pre><code>import novax as nx\nimport numpy as np\n\nnp.random.seed(42)\nx  = nx.Tensor(np.random.randn(32, 16).astype(np.float32))\nW1 = nx.Tensor(np.random.randn(16, 8).astype(np.float32), requires_grad=True)\nb1 = nx.Tensor(np.zeros(8, dtype=np.float32),             requires_grad=True)\nW2 = nx.Tensor(np.random.randn(8, 4).astype(np.float32),  requires_grad=True)\nb2 = nx.Tensor(np.zeros(4, dtype=np.float32),             requires_grad=True)\n\n# Hidden layer with ReLU, output with sigmoid\nh   = nx.relu(nx.matmul(x, W1) + b1)    # (32, 8)\nout = nx.sigmoid(nx.matmul(h, W2) + b2)  # (32, 4)\n\nloss = nx.mean(out)\nloss.eval().backward()\n</code></pre>"},{"location":"api/autograd/","title":"Autograd","text":"<p>NovaX implements reverse-mode automatic differentiation (backpropagation). Gradients are computed lazily: the forward pass builds a computation graph, and <code>.backward()</code> traverses it in reverse to accumulate gradients.</p>"},{"location":"api/autograd/#requires_grad","title":"<code>requires_grad</code>","text":"<p>Mark a tensor as a learnable parameter by setting <code>requires_grad=True</code>:</p> <pre><code>W = nx.Tensor(np.random.randn(8, 4).astype(np.float32), requires_grad=True)\nb = nx.Tensor(np.zeros(4, dtype=np.float32),             requires_grad=True)\n</code></pre> <p>Any operation whose result depends on a <code>requires_grad=True</code> tensor will automatically set <code>requires_grad=True</code> on the output and attach a backward closure.</p> <pre><code>x = nx.Tensor(np.ones((3, 8), dtype=np.float32))  # no grad\nh = nx.relu(x @ W + b)                             # h.requires_grad is True\n</code></pre>"},{"location":"api/autograd/#tensorgrad","title":"<code>Tensor.grad</code>","text":"<p>After calling <code>.backward()</code>, each leaf tensor with <code>requires_grad=True</code> has its <code>.grad</code> attribute set to a <code>Tensor</code> containing the accumulated gradient.</p> <pre><code>W.grad          # Tensor with shape (8, 4)\nW.grad.data     # np.ndarray\n</code></pre> <p>Gradients accumulate across multiple backward calls. Zero them manually between optimisation steps:</p> <pre><code># Gradient descent step\nfor p in [W, b]:\n    p.data -= lr * p.grad.data\n    p.grad = None   # reset for next iteration\n</code></pre>"},{"location":"api/autograd/#tensorbackward","title":"<code>Tensor.backward()</code>","text":"<pre><code>tensor.backward() -&gt; None\n</code></pre> <p>Run reverse-mode autodiff from this tensor.</p> <ol> <li>Seeds <code>self.grad = Tensor(np.ones(self.shape))</code>.</li> <li>Performs a topological sort of the graph via the <code>_prev</code> dependency sets.</li> <li>Calls each node's <code>_backward</code> closure in reverse topological order, accumulating    gradients via <code>+=</code>.</li> </ol> <p>Usage</p> <p>Call on a scalar (loss) tensor after <code>.eval()</code>:</p> <pre><code>loss = nx.mean(nx.relu(x @ W + b))\nloss.eval().backward()\n</code></pre> <p>Warning</p> <p>Always call <code>.eval()</code> before <code>.backward()</code>. <code>backward()</code> expects a concrete tensor \u2014 it cannot be called on a lazy graph node.</p>"},{"location":"api/autograd/#novaxno_grad","title":"<code>novax.no_grad</code>","text":"<pre><code>with novax.no_grad():\n    ...\n</code></pre> <p>Context manager that disables gradient tracking within its body. Operations executed inside the block do not attach backward closures, saving memory and computation.</p> <p>Useful for inference, validation loops, and when freezing parts of a model.</p> <pre><code># Training step \u2014 track gradients\nh    = nx.relu(nx.matmul(x, W) + b)\nloss = nx.mean(h)\nloss.eval().backward()\n\n# Inference \u2014 no gradients needed\nwith nx.no_grad():\n    pred = nx.softmax(nx.matmul(x_val, W) + b).eval()\n</code></pre> <p>Gradient tracking is automatically restored when the <code>with</code> block exits, even if an exception is raised.</p> <pre><code>a = nx.Tensor([1.0], requires_grad=True)\nwith nx.no_grad():\n    c = (a + 1.0).eval()\n    print(c.requires_grad)   # False\n\nd = (a + 1.0).eval()\nprint(d.requires_grad)       # True  \u2014 restored\n</code></pre>"},{"location":"api/autograd/#gradient-rules","title":"Gradient Rules","text":""},{"location":"api/autograd/#elementwise-binary-ops","title":"Elementwise binary ops","text":"Op \\(\\partial L / \\partial a\\) \\(\\partial L / \\partial b\\) <code>add</code> \\(g\\) \\(g\\) <code>sub</code> \\(g\\) \\(-g\\) <code>mul</code> \\(g \\cdot b\\) \\(g \\cdot a\\) <code>div</code> \\(g / b\\) \\(-g \\cdot a / b^2\\) <code>pow</code> \\(g \\cdot b \\cdot a^{b-1}\\) \\(g \\cdot a^b \\cdot \\ln(a)\\)"},{"location":"api/autograd/#unary-ops","title":"Unary ops","text":"Op \\(\\partial L / \\partial a\\) <code>neg</code> \\(-g\\) <code>exp</code> \\(g \\cdot e^a\\) <code>log</code> \\(g / a\\) <code>sqrt</code> \\(g / (2\\sqrt{a})\\) <code>abs</code> \\(g \\cdot \\text{sign}(a)\\) <code>relu</code> \\(g \\cdot \\mathbf{1}[a &gt; 0]\\) <code>sigmoid</code> \\(g \\cdot \\sigma(a)(1 - \\sigma(a))\\) <code>tanh</code> \\(g \\cdot (1 - \\tanh^2(a))\\)"},{"location":"api/autograd/#reductions","title":"Reductions","text":"Op \\(\\partial L / \\partial a_i\\) <code>sum</code> \\(g\\) <code>mean</code> \\(g / n\\) <code>max</code> \\(g \\cdot \\mathbf{1}[a_i = \\max(a)] / \\text{count}(\\max)\\) <code>min</code> \\(g \\cdot \\mathbf{1}[a_i = \\min(a)] / \\text{count}(\\min)\\)"},{"location":"api/autograd/#matmul","title":"Matmul","text":"\\[\\frac{\\partial L}{\\partial A} = g \\cdot B^\\top, \\quad \\frac{\\partial L}{\\partial B} = A^\\top \\cdot g\\]"},{"location":"api/autograd/#broadcasting-and-gradients","title":"Broadcasting and Gradients","text":"<p>When a lower-rank tensor is broadcast to a higher-rank shape during a forward op, NovaX automatically sums the gradient back to the original shape during backward.</p> <pre><code>W = nx.Tensor(np.random.randn(4, 8).astype(np.float32), requires_grad=True)\nb = nx.Tensor(np.zeros(8, dtype=np.float32),             requires_grad=True)\nx = nx.Tensor(np.ones((32, 4), dtype=np.float32))\n\n# b (shape (8,)) is broadcast across the batch dimension\nh = nx.matmul(x, W) + b  # h has shape (32, 8)\nnx.mean(h).eval().backward()\n\nprint(b.grad.shape)   # (8,)  \u2014 NOT (32, 8)\n</code></pre> <p>The unbroadcast step sums over any leading axes that were added during broadcast and over any axis where the original size was 1.</p>"},{"location":"api/autograd/#complete-training-loop-example","title":"Complete Training Loop Example","text":"<pre><code>import novax as nx\nimport numpy as np\n\nnp.random.seed(0)\n\n# Toy dataset: y = sign(sum(x))\nX = nx.Tensor(np.random.randn(64, 8).astype(np.float32))\nY = nx.Tensor((np.random.randn(64, 1) &gt; 0).astype(np.float32))\n\n# Parameters\nW1 = nx.Tensor(np.random.randn(8, 16).astype(np.float32) * 0.1, requires_grad=True)\nb1 = nx.Tensor(np.zeros(16, dtype=np.float32),                   requires_grad=True)\nW2 = nx.Tensor(np.random.randn(16, 1).astype(np.float32) * 0.1, requires_grad=True)\nb2 = nx.Tensor(np.zeros(1, dtype=np.float32),                    requires_grad=True)\n\nlr = 0.01\nparams = [W1, b1, W2, b2]\n\nfor step in range(100):\n    # Forward\n    h    = nx.relu(nx.matmul(X, W1) + b1)\n    logit = nx.matmul(h, W2) + b2\n    pred  = nx.sigmoid(logit)\n    loss  = nx.mean((pred - Y) ** 2)  # MSE\n\n    # Backward\n    loss.eval().backward()\n\n    # SGD update\n    for p in params:\n        if p.grad is not None:\n            p.data -= lr * p.grad.data\n            p.grad = None\n\n    if step % 20 == 0:\n        print(f\"step {step:3d}  loss={float(loss.data[0]):.4f}\")\n</code></pre>"},{"location":"api/linalg/","title":"Linear Algebra","text":""},{"location":"api/linalg/#novaxmatmul","title":"<code>novax.matmul</code>","text":"<pre><code>novax.matmul(a, b) -&gt; Tensor\n</code></pre> <p>Matrix multiplication of two 2-D tensors.</p> \\[C = A \\cdot B \\quad (M \\times K) \\cdot (K \\times N) \\to (M \\times N)\\] <p>Can also be written using the <code>@</code> operator:</p> <pre><code>C = A @ B\n</code></pre> <p>Parameters</p> Parameter Type Description <code>a</code> <code>Tensor</code> Left matrix, shape <code>(M, K)</code> <code>b</code> <code>Tensor</code> Right matrix, shape <code>(K, N)</code> <p>Returns <code>Tensor</code> of shape <code>(M, N)</code>.</p> <p>Raises <code>ValueError</code> if either input is not 2-D.</p>"},{"location":"api/linalg/#examples","title":"Examples","text":"<pre><code>import novax as nx\nimport numpy as np\n\nA = nx.Tensor(np.array([[1.0, 2.0],\n                         [3.0, 4.0]]))\nB = nx.Tensor(np.array([[5.0, 6.0],\n                         [7.0, 8.0]]))\n\nC = nx.matmul(A, B)\nprint(C.eval().data)\n# [[19. 22.]\n#  [43. 50.]]\n\n# Operator syntax\nC2 = (A @ B).eval()\n</code></pre>"},{"location":"api/linalg/#gpu-kernel-tiled-cuda-matmul","title":"GPU kernel \u2014 tiled CUDA matmul","text":"<p>On GPU, NovaX uses a 16\u00d716 tiled matrix multiplication with shared memory:</p> <pre><code>#define TILE 16\n__shared__ float As[TILE][TILE];\n__shared__ float Bs[TILE][TILE];\n\n// Each thread block computes a TILE\u00d7TILE tile of C.\n// Inner loop loads tiles of A and B into shared memory,\n// accumulates the dot product, then advances the tile window.\n</code></pre> <p>Shared memory tiling reduces global memory accesses by a factor of <code>TILE</code> compared to a naive kernel, greatly increasing arithmetic intensity.</p> <p>Boundary conditions are handled so non-multiple-of-16 shapes work correctly.</p>"},{"location":"api/linalg/#autograd","title":"Autograd","text":"<p><code>matmul</code> supports full reverse-mode differentiation for both inputs.</p> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial A} = \\frac{\\partial L}{\\partial C} \\cdot B^\\top, \\quad \\frac{\\partial L}{\\partial B} = A^\\top \\cdot \\frac{\\partial L}{\\partial C}\\] <pre><code>A = nx.Tensor(np.random.randn(4, 8).astype(np.float32), requires_grad=True)\nB = nx.Tensor(np.random.randn(8, 4).astype(np.float32), requires_grad=True)\n\nloss = nx.mean(A @ B)\nloss.eval().backward()\n\nprint(A.grad.shape)   # (4, 8)\nprint(B.grad.shape)   # (8, 4)\n</code></pre> <p>Only inputs with <code>requires_grad=True</code> accumulate a gradient \u2014 if <code>B</code> is a frozen weight, set <code>requires_grad=False</code> and its gradient will not be computed.</p>"},{"location":"api/linalg/#novaxlaunch_matmul_bias_relu","title":"<code>novax.launch_matmul_bias_relu</code>","text":"<pre><code>novax.launch_matmul_bias_relu(x, w, bias) -&gt; Tensor\n</code></pre> <p>Fused GPU kernel that performs matmul + bias addition + ReLU activation in a single CUDA pass, saving two global memory round-trips compared to chaining three separate ops.</p> \\[\\text{out} = \\text{relu}(X \\cdot W + \\text{bias})\\] <p>Parameters</p> Parameter Type Description <code>x</code> <code>Tensor</code> Input, shape <code>(M, K)</code>, must be on GPU <code>w</code> <code>Tensor</code> Weights, shape <code>(K, N)</code>, must be on GPU <code>bias</code> <code>Tensor</code> Bias vector, shape <code>(N,)</code>, must be on GPU <p>Returns <code>Tensor</code> of shape <code>(M, N)</code>, on GPU.</p> <p>GPU only</p> <p>This function requires all three tensors to reside in GPU memory. No CPU fallback is provided.</p> <p>Autograd</p> <p><code>launch_matmul_bias_relu</code> is a low-level performance primitive and does not attach autograd backward closures. Use it in inference or when you handle gradients manually.</p> <pre><code>if nx.GPU_AVAILABLE:\n    M, K, N = 64, 128, 32\n    x    = nx.Tensor(np.random.randn(M, K).astype(np.float32)).to_gpu()\n    w    = nx.Tensor(np.random.randn(K, N).astype(np.float32)).to_gpu()\n    bias = nx.Tensor(np.zeros(N, dtype=np.float32)).to_gpu()\n\n    out = nx.launch_matmul_bias_relu(x, w, bias)\n    print(out.to_host().shape)   # (64, 32)\n</code></pre>"},{"location":"api/linalg/#performance-notes","title":"Performance Notes","text":"Matrix size NovaX GPU (tiled) NumPy CPU 128 \u00d7 128 ~0.05 ms ~0.1 ms 512 \u00d7 512 ~0.3 ms ~4 ms 1024 \u00d7 1024 ~1.2 ms ~30 ms <p>Indicative timings on an NVIDIA RTX 3080 vs. AMD Ryzen 9 5900X. Actual performance depends heavily on hardware.</p> <p>For production-scale matmul, consider cuBLAS (via PyCUDA or PyTorch) which uses highly tuned GEMM implementations. NovaX's tiled kernel is a clean educational implementation that already beats NumPy at larger sizes.</p>"},{"location":"api/math/","title":"Math Operations","text":"<p>Elementwise math functions. All functions accept a <code>Tensor</code> and return a new <code>Tensor</code>. CPU and GPU execution are handled automatically.</p>"},{"location":"api/math/#arithmetic","title":"Arithmetic","text":""},{"location":"api/math/#novaxadd","title":"<code>novax.add</code>","text":"<pre><code>novax.add(a, b) -&gt; Tensor\n</code></pre> <p>Elementwise addition. Equivalent to <code>a + b</code>.</p> <p>Parameters</p> Parameter Type Description <code>a</code> <code>Tensor</code> Left operand <code>b</code> <code>Tensor</code> Right operand <pre><code>a = nx.Tensor([1.0, 2.0, 3.0])\nb = nx.Tensor([10.0, 20.0, 30.0])\nnx.add(a, b).eval().data   # [11. 22. 33.]\n</code></pre> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}}, \\quad \\frac{\\partial L}{\\partial b} = \\frac{\\partial L}{\\partial \\text{out}}\\]"},{"location":"api/math/#novaxsub","title":"<code>novax.sub</code>","text":"<pre><code>novax.sub(a, b) -&gt; Tensor\n</code></pre> <p>Elementwise subtraction. Equivalent to <code>a - b</code>.</p> <pre><code>nx.sub(a, b).eval().data   # [-9. -18. -27.]\n</code></pre> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}}, \\quad \\frac{\\partial L}{\\partial b} = -\\frac{\\partial L}{\\partial \\text{out}}\\]"},{"location":"api/math/#novaxmul","title":"<code>novax.mul</code>","text":"<pre><code>novax.mul(a, b) -&gt; Tensor\n</code></pre> <p>Elementwise multiplication. Equivalent to <code>a * b</code>.</p> <pre><code>nx.mul(a, b).eval().data   # [10. 40. 90.]\n</code></pre> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}} \\cdot b, \\quad \\frac{\\partial L}{\\partial b} = \\frac{\\partial L}{\\partial \\text{out}} \\cdot a\\]"},{"location":"api/math/#novaxdiv","title":"<code>novax.div</code>","text":"<pre><code>novax.div(a, b) -&gt; Tensor\n</code></pre> <p>Elementwise division. Equivalent to <code>a / b</code>.</p> <pre><code>nx.div(a, b).eval().data   # [0.1  0.1  0.1]\n</code></pre> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}}  / b, \\quad \\frac{\\partial L}{\\partial b} = -\\frac{\\partial L}{\\partial \\text{out}} \\cdot \\frac{a}{b^2}\\]"},{"location":"api/math/#novaxpow","title":"<code>novax.pow</code>","text":"<pre><code>novax.pow(a, b) -&gt; Tensor\n</code></pre> <p>Elementwise power. Equivalent to <code>a ** b</code>.</p> <pre><code>base = nx.Tensor([2.0, 3.0, 4.0])\nexp  = nx.Tensor([3.0, 2.0, 0.5])\nnx.pow(base, exp).eval().data   # [8.  9.  2.]\n</code></pre> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}} \\cdot b \\cdot a^{b-1}, \\quad \\frac{\\partial L}{\\partial b} = \\frac{\\partial L}{\\partial \\text{out}} \\cdot a^b \\cdot \\ln(a)\\]"},{"location":"api/math/#novaxneg","title":"<code>novax.neg</code>","text":"<pre><code>novax.neg(a) -&gt; Tensor\n</code></pre> <p>Elementwise negation. Equivalent to <code>-a</code>.</p> <pre><code>nx.neg(nx.Tensor([1.0, -2.0, 3.0])).eval().data   # [-1.  2. -3.]\n</code></pre> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = -\\frac{\\partial L}{\\partial \\text{out}}\\]"},{"location":"api/math/#exponential-and-logarithm","title":"Exponential and Logarithm","text":""},{"location":"api/math/#novaxexp","title":"<code>novax.exp</code>","text":"<pre><code>novax.exp(a) -&gt; Tensor\n</code></pre> <p>Elementwise natural exponential: \\(e^a\\).</p> <pre><code>nx.exp(nx.Tensor([0.0, 1.0, 2.0])).eval().data   # [1.     2.718  7.389]\n</code></pre> <p>GPU kernel: <code>expf(a[idx])</code></p> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}} \\cdot e^a\\]"},{"location":"api/math/#novaxlog","title":"<code>novax.log</code>","text":"<pre><code>novax.log(a) -&gt; Tensor\n</code></pre> <p>Elementwise natural logarithm: \\(\\ln(a)\\).</p> <p>Domain</p> <p>Input values must be strictly positive. Passing non-positive values raises <code>ValueError</code> on CPU. GPU behavior is undefined (CUDA returns <code>-inf</code> or <code>nan</code>).</p> <pre><code>nx.log(nx.Tensor([1.0, 2.0, 4.0])).eval().data   # [0.     0.693  1.386]\n</code></pre> <p>GPU kernel: <code>logf(a[idx])</code></p> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}} / a\\]"},{"location":"api/math/#roots-and-absolute-value","title":"Roots and Absolute Value","text":""},{"location":"api/math/#novaxsqrt","title":"<code>novax.sqrt</code>","text":"<pre><code>novax.sqrt(a) -&gt; Tensor\n</code></pre> <p>Elementwise square root: \\(\\sqrt{a}\\).</p> <p>Domain</p> <p>Negative inputs raise <code>ValueError</code> on CPU.</p> <pre><code>nx.sqrt(nx.Tensor([1.0, 4.0, 9.0])).eval().data   # [1. 2. 3.]\n</code></pre> <p>GPU kernel: <code>sqrtf(a[idx])</code></p> <p>Gradient (numerically stabilised to avoid division by zero)</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}}}{2\\sqrt{\\max(a,\\, 10^{-12})}}\\]"},{"location":"api/math/#novaxabs","title":"<code>novax.abs</code>","text":"<pre><code>novax.abs(a) -&gt; Tensor\n</code></pre> <p>Elementwise absolute value: \\(|a|\\).</p> <pre><code>nx.abs(nx.Tensor([-3.0, 0.0, 4.0])).eval().data   # [3. 0. 4.]\n</code></pre> <p>GPU kernel: <code>fabsf(a[idx])</code></p> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a} = \\frac{\\partial L}{\\partial \\text{out}} \\cdot \\text{sign}(a)\\]"},{"location":"api/math/#broadcasting","title":"Broadcasting","text":"<p>All binary ops (<code>add</code>, <code>sub</code>, <code>mul</code>, <code>div</code>, <code>pow</code>) support broadcasting following NumPy semantics. When gradients flow backward through a broadcast, NovaX automatically sums the gradient over the broadcast axes to restore the original shape.</p> <pre><code>a = nx.Tensor(np.ones((4, 3), dtype=np.float32), requires_grad=True)\nb = nx.Tensor(np.array([1.0, 2.0, 3.0]),          requires_grad=True)\n\nloss = nx.sum(a + b)  # b is broadcast from (3,) \u2192 (4, 3)\nloss.eval().backward()\n\nprint(a.grad.shape)   # (4, 3)\nprint(b.grad.shape)   # (3,)  \u2014 summed over broadcast dim\n</code></pre>"},{"location":"api/reductions/","title":"Reduction Operations","text":"<p>Reduction functions collapse a tensor to a smaller tensor (usually a scalar). All reductions currently operate over all elements of the input tensor.</p>"},{"location":"api/reductions/#novaxsum","title":"<code>novax.sum</code>","text":"<pre><code>novax.sum(a) -&gt; Tensor\n</code></pre> <p>Sum of all elements. Returns a scalar tensor of shape <code>(1,)</code>.</p> \\[\\text{sum}(x) = \\sum_i x_i\\] <pre><code>a = nx.Tensor([1.0, 2.0, 3.0, 4.0])\nnx.sum(a).eval().data   # [10.]\n</code></pre> <p>GPU implementation</p> <p>Two-pass parallel tree reduction using 256-thread blocks with shared memory:</p> <pre><code>Pass 1: each block reduces its chunk to a partial sum  \u2192 partial[] in VRAM\nPass 2: second kernel reduces partial[] to a scalar\n</code></pre> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a_i} = \\frac{\\partial L}{\\partial \\text{out}}\\] <p>Gradient is broadcast back to the full input shape (all-ones scaled by the upstream gradient).</p> <pre><code>a = nx.Tensor([1.0, 2.0, 3.0], requires_grad=True)\nnx.sum(a).eval().backward()\nprint(a.grad.data)   # [1. 1. 1.]\n</code></pre>"},{"location":"api/reductions/#novaxmean","title":"<code>novax.mean</code>","text":"<pre><code>novax.mean(a) -&gt; Tensor\n</code></pre> <p>Arithmetic mean of all elements. Returns a scalar tensor of shape <code>(1,)</code>.</p> \\[\\text{mean}(x) = \\frac{1}{n} \\sum_i x_i\\] <pre><code>a = nx.Tensor([1.0, 2.0, 3.0, 4.0])\nnx.mean(a).eval().data   # [2.5]\n</code></pre> <p>GPU implementation</p> <p>Computes <code>sum(a) / n</code> where the division is a scalar CUDA kernel applied to the result of the two-pass reduction.</p> <p>Gradient</p> \\[\\frac{\\partial L}{\\partial a_i} = \\frac{\\partial L}{\\partial \\text{out}}}{n}\\] <pre><code>a = nx.Tensor([1.0, 2.0, 3.0, 4.0], requires_grad=True)\nnx.mean(a).eval().backward()\nprint(a.grad.data)   # [0.25 0.25 0.25 0.25]\n</code></pre>"},{"location":"api/reductions/#novaxmax","title":"<code>novax.max</code>","text":"<pre><code>novax.max(a) -&gt; Tensor\n</code></pre> <p>Maximum value across all elements. Returns a scalar tensor of shape <code>(1,)</code>.</p> <pre><code>a = nx.Tensor([3.0, 1.0, 4.0, 1.0, 5.0])\nnx.max(a).eval().data   # [5.]\n</code></pre> <p>GPU implementation</p> <p>Two-pass tree reduction with <code>fmaxf(a, b)</code> as the reduction operator.</p> <p>Gradient</p> <p>Gradient flows back through the maximum value(s). If multiple elements share the maximum, the gradient is split equally.</p> \\[\\frac{\\partial L}{\\partial a_i} = \\frac{\\partial L}{\\partial \\text{out}} \\cdot \\frac{\\mathbf{1}[a_i = \\max(a)]}{\\text{count}(\\max)}\\]"},{"location":"api/reductions/#novaxmin","title":"<code>novax.min</code>","text":"<pre><code>novax.min(a) -&gt; Tensor\n</code></pre> <p>Minimum value across all elements. Returns a scalar tensor of shape <code>(1,)</code>.</p> <pre><code>a = nx.Tensor([3.0, 1.0, 4.0, 1.0, 5.0])\nnx.min(a).eval().data   # [1.]\n</code></pre> <p>GPU implementation</p> <p>Two-pass tree reduction with <code>fminf(a, b)</code> as the reduction operator.</p> <p>Gradient</p> <p>Symmetric to <code>max</code> \u2014 gradient flows through the minimum value(s).</p>"},{"location":"api/reductions/#common-patterns","title":"Common Patterns","text":""},{"location":"api/reductions/#loss-functions","title":"Loss functions","text":"<pre><code># Mean Squared Error\ndef mse(pred, target):\n    diff = pred - target\n    return nx.mean(diff ** 2)\n\n# Binary Cross-Entropy (numerically simplified)\ndef bce(logits, y):\n    return nx.mean(nx.log(nx.sigmoid(logits)) * y +\n                   nx.log(1.0 - nx.sigmoid(logits)) * (1.0 - y))\n</code></pre>"},{"location":"api/reductions/#normalisation","title":"Normalisation","text":"<pre><code>x     = nx.Tensor(np.random.randn(128).astype(np.float32))\nmu    = nx.mean(x).eval()\nsigma = nx.sqrt(nx.mean((x - float(mu.data[0])) ** 2)).eval()\nx_norm = (x - float(mu.data[0])) / (float(sigma.data[0]) + 1e-5)\n</code></pre> <p>Axis reductions coming in v0.3.0</p> <p>Current reductions always flatten the entire tensor. Per-axis reductions (<code>axis=0</code>, <code>axis=1</code>, <code>keepdims=True</code>) are planned for a future release.</p>"},{"location":"api/tensor/","title":"Tensor","text":"<pre><code>class novax.Tensor(data, *, requires_grad=False)\n</code></pre> <p>The core data structure in NovaX. A <code>Tensor</code> can hold CPU data (a NumPy array), GPU data (a PyCUDA device pointer), or be an unevaluated node in a lazy expression graph.</p>"},{"location":"api/tensor/#constructor","title":"Constructor","text":"<pre><code>nx.Tensor(data, requires_grad=False)\n</code></pre> <p>Parameters</p> Parameter Type Description <code>data</code> <code>ndarray</code>, <code>list</code>, <code>float</code>, <code>int</code>, or <code>None</code> Initial data. <code>None</code> is used internally for lazy graph nodes. <code>requires_grad</code> <code>bool</code> When <code>True</code>, gradients are tracked through operations involving this tensor. Default <code>False</code>. <p>Examples</p> <pre><code>import novax as nx\nimport numpy as np\n\n# From a NumPy array\na = nx.Tensor(np.array([1.0, 2.0, 3.0]))\n\n# From a Python list \u2014 converted to float32 automatically\nb = nx.Tensor([4.0, 5.0, 6.0])\n\n# Scalar (shape becomes (1,))\nc = nx.Tensor(3.14)\n\n# With gradient tracking\nW = nx.Tensor(np.random.randn(4, 4).astype(np.float32), requires_grad=True)\n</code></pre> <p>dtype</p> <p>All data is stored as <code>float32</code>. Integer inputs are silently converted.</p>"},{"location":"api/tensor/#attributes","title":"Attributes","text":"Attribute Type Description <code>data</code> <code>np.ndarray</code> or <code>None</code> Host array. <code>None</code> when the tensor is on GPU or unevaluated. <code>shape</code> <code>tuple</code> Shape of the tensor data. <code>size</code> <code>int</code> Total number of elements. <code>dtype</code> <code>np.dtype</code> Always <code>float32</code>. <code>on_gpu</code> <code>bool</code> <code>True</code> when data resides in VRAM. <code>is_leaf</code> <code>bool</code> <code>True</code> for tensors created directly from data (not from an op). <code>requires_grad</code> <code>bool</code> Whether gradients are accumulated for this tensor. <code>grad</code> <code>Tensor</code> or <code>None</code> Accumulated gradient after calling <code>backward()</code>. Initially <code>None</code>. <code>op</code> <code>str</code> or <code>None</code> The operation that produced this tensor (<code>\"add\"</code>, <code>\"relu\"</code>, \u2026). <code>None</code> for leaf tensors."},{"location":"api/tensor/#device-management","title":"Device Management","text":""},{"location":"api/tensor/#to_gpu","title":"<code>to_gpu()</code>","text":"<pre><code>tensor.to_gpu() -&gt; Tensor\n</code></pre> <p>Upload tensor data from host memory to GPU (VRAM). Returns <code>self</code> for chaining.</p> <p>Raises</p> <ul> <li><code>RuntimeError</code> \u2014 if no GPU is available (<code>GPU_AVAILABLE</code> is <code>False</code>)</li> <li><code>ValueError</code> \u2014 if the tensor has no host data to upload</li> </ul> <pre><code>a = nx.Tensor(np.ones(1024, dtype=np.float32))\na.to_gpu()                 # a.on_gpu is now True, a.data is None\nresult = nx.exp(a).eval()  # runs CUDA kernel\n</code></pre>"},{"location":"api/tensor/#to_host","title":"<code>to_host()</code>","text":"<pre><code>tensor.to_host() -&gt; np.ndarray\n</code></pre> <p>Download and return the tensor's data as a NumPy array. Does not modify the tensor \u2014 the buffer stays on GPU.</p> <pre><code>a.to_gpu()\narr = a.to_host()   # np.ndarray, a.on_gpu still True\n</code></pre> <p>If the tensor is already on CPU, returns <code>self.data</code> directly.</p>"},{"location":"api/tensor/#shape-manipulation","title":"Shape Manipulation","text":""},{"location":"api/tensor/#reshapenew_shape","title":"<code>reshape(*new_shape)</code>","text":"<pre><code>tensor.reshape(*new_shape) -&gt; Tensor\n</code></pre> <p>Return a new tensor with data reshaped. Downloads from GPU if necessary.</p> <pre><code>a = nx.Tensor(np.arange(12, dtype=np.float32))\nb = a.reshape(3, 4)\nprint(b.shape)   # (3, 4)\n</code></pre>"},{"location":"api/tensor/#transposeaxesnone","title":"<code>transpose(axes=None)</code>","text":"<pre><code>tensor.transpose(axes=None) -&gt; Tensor\n</code></pre> <p>Return a transposed tensor. Follows NumPy axis convention. Downloads from GPU if necessary.</p> <pre><code>a = nx.Tensor(np.ones((2, 3), dtype=np.float32))\nb = a.transpose()\nprint(b.shape)   # (3, 2)\n\n# With explicit axis order\nc = nx.Tensor(np.ones((2, 3, 4), dtype=np.float32))\nd = c.transpose(axes=(2, 0, 1))\nprint(d.shape)   # (4, 2, 3)\n</code></pre>"},{"location":"api/tensor/#evaluation","title":"Evaluation","text":""},{"location":"api/tensor/#eval","title":"<code>eval()</code>","text":"<pre><code>tensor.eval() -&gt; Tensor\n</code></pre> <p>Compile and execute the expression graph rooted at this tensor. Returns a new concrete <code>Tensor</code> with <code>data</code> set (CPU) or <code>gpu_ptr</code> set (GPU).</p> <p>Calling <code>eval()</code> on a leaf tensor returns <code>self</code> immediately.</p> <pre><code>a = nx.Tensor([1.0, 2.0])\nb = nx.Tensor([3.0, 4.0])\n\n# Graph built lazily\nexpr = nx.relu(a + b * 2.0)   # no computation yet\n\n# Execute\nresult = expr.eval()\nprint(result.data)   # [7. 8.]\n</code></pre> <p>Always call eval() before backward()</p> <p><code>backward()</code> expects a concrete scalar tensor (the loss). Always call <code>.eval()</code> on your loss before calling <code>.backward()</code>.</p>"},{"location":"api/tensor/#autograd","title":"Autograd","text":""},{"location":"api/tensor/#backward","title":"<code>backward()</code>","text":"<pre><code>tensor.backward() -&gt; None\n</code></pre> <p>Run reverse-mode automatic differentiation from this tensor. Seeds <code>self.grad</code> with an all-ones tensor, then propagates gradients backward through the computation graph in topological order.</p> <p>Conditions</p> <ul> <li>This tensor must be a scalar (or at least the result of a reduction) for   the default seeding to be meaningful.</li> <li>Leaf tensors with <code>requires_grad=True</code> will have their <code>.grad</code> attribute set.</li> </ul> <pre><code>W = nx.Tensor(np.random.randn(3, 3).astype(np.float32), requires_grad=True)\nb = nx.Tensor(np.zeros(3, dtype=np.float32), requires_grad=True)\nx = nx.Tensor(np.ones((5, 3), dtype=np.float32))\n\nloss = nx.mean(nx.relu(nx.matmul(x, W) + b))\nloss.eval().backward()\n\nprint(W.grad.shape)   # (3, 3)\nprint(b.grad.shape)   # (3,)\n</code></pre>"},{"location":"api/tensor/#operator-overloading","title":"Operator Overloading","text":"<p>Standard Python arithmetic operators are overloaded to build lazy graph nodes.</p> Expression Operation Description <code>a + b</code> add Elementwise addition <code>a - b</code> sub Elementwise subtraction <code>a * b</code> mul Elementwise multiplication <code>a / b</code> div Elementwise division <code>a ** b</code> pow Elementwise power <code>a @ b</code> matmul Matrix multiplication <code>-a</code> neg Negation <p>Scalar operands (<code>int</code>, <code>float</code>) are automatically wrapped in a <code>Tensor</code>.</p> <pre><code>a = nx.Tensor([1.0, 2.0, 3.0])\n\n(a + 1).eval().data        # [2. 3. 4.]\n(a * 0.5).eval().data      # [0.5 1.  1.5]\n(2.0 - a).eval().data      # [1.  0. -1.]\n(a ** 2).eval().data       # [1. 4. 9.]\n(-a).eval().data           # [-1. -2. -3.]\n</code></pre>"},{"location":"api/tensor/#memory-management","title":"Memory Management","text":""},{"location":"api/tensor/#freereleasefalse","title":"<code>free(release=False)</code>","text":"<pre><code>tensor.free(release=False) -&gt; None\n</code></pre> <p>Return the GPU buffer to the memory pool. Set <code>release=True</code> to release memory back to the CUDA driver rather than caching it.</p> <pre><code>a.to_gpu()\n# ... use a ...\na.free()           # returns buffer to pool for reuse\na.free(release=True)   # returns memory to CUDA driver\n</code></pre>"},{"location":"api/tensor/#context-manager","title":"Context Manager","text":"<p><code>Tensor</code> can be used as a context manager; the GPU buffer is automatically freed when the <code>with</code> block exits.</p> <pre><code>with nx.Tensor(np.ones(1024, dtype=np.float32)) as t:\n    t.to_gpu()\n    result = nx.sigmoid(t).eval()\n# t.free() called automatically\n</code></pre>"},{"location":"api/tensor/#representation","title":"Representation","text":"<pre><code>&gt;&gt;&gt; nx.Tensor(np.array([1.0, 2.0]), requires_grad=True)\nTensor(shape=(2,), device=CPU, op=None, leaf=True, grad=False)\n</code></pre>"}]}